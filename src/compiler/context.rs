// Translation context for a single compilation run.
//
// This module is reserved for future per-target compilation state.
// `CompileContext` and `CompilePlan` have been removed — they were unused
// scaffolding. `TargetRegistry` is constructed directly in
// `compile_config_entries` where it is needed.
